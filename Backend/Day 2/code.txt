DEMO SQL & foreign keys

- CREATE ROLE
- CREATE DATABASE

- DROP DATABSE IF EXISTS devs

- CREATE TABLE MYCOMPANY(
    IN INT PRIMARY KEY NOT NULL,
    NAME TEXT NOT NULL,
    ADDRESS CHAR[100],
    SALARY REAL,
);

- CREATE SCHEMA mySchema
- CREATE TABLE mySchema.mycompany(
    ID INT NOT NULL,
    NAME VARCHAR[20] NOT NULL,
    AGE INT NOT NULL,
    ADDRESS CHAR[25],
    SALARY DECIMAL(18,2),
    PRIMARY KEY(ID)
);

- \d mycompany

- INSERT INTO MYCOMPANY(ID, NAME,AGE...)